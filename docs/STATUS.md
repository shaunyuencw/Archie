# Engineering checkpoint — 22 September 2026, Mac continuation

Mac presentation-prototype checkpoint. M0–M5 pass; M6 cross-platform exports and M7 Mac handover verified. Native Visio acceptance remains open. Estimated guided-demo readiness 90%; this is a judgement, not test coverage or production readiness. See `PROGRESS.md` and `docs/ACCEPTANCE.md`.

Earlier Mac checkpoint: pnpm/Node/Python startup portability, stale-view cancellation and serialized local commands, backend redo/branch invalidation, canvas keyboard and clipboard/group edits, 16-unit snapping with Option bypass, explicit zone reassignment, draggable routes and dependable endpoint hit targets, inferred directional handles, non-overlapping external defaults, collapsible/resizable persisted panels/focus/search, compact visual hierarchy, deferred ELK loading, updated SVG/DOCX rendering, conservative provider scope evidence guard.

Earlier offline gate: **145 backend, 33 frontend unit, 33 browser journeys, TypeScript, production build — passed**. Existing tests were retained. Additional evidence includes actual pointer groups, snapping, undo/redo/copy/paste/delete, input shortcut isolation, route/prompt preservation, zero model calls for canvas work and 50/100 capacity. Initial Mac baseline failure and previous Windows results retained separately. The latest citation-fix gate below has a browser resize failure.

Product feedback follow-up: whole connector segment dragging and right-click line styles; global 42-clause fictional policy library with project applicability, custom drafts and undo; Production portal and hybrid robotics Production/Testbed/AWS references; 43 grouped symbols; declared instance stacks and hosted virtual-firewall badges; simpler view names, readable Overview, editable Connections and explained Sources. Four synthetic specs and 15 follow-up prompts are in `docs/DEMO_SCENARIOS.md`. New source/narrative visual QA covers 22 pages plus six SVGs (`reports/demo-pack-qa/review.json`). New packs are verified through authored Mock fixtures; their general natural-language live workflows remain unverified. One new Terra advisory policy review passed for three portal policies, costing an estimated $0.01538 with no architecture edits (`reports/mac-policy-review-20260921.json`). Prepared portal/robotics layouts occupy 47%/48% less area, with 36%/48% larger fitted labels; saved positions are preserved. Narrow 640×800 layouts use overlay panels. Project Trash/restore preserves edit history; confirmed Empty Trash permanently purges content while retaining spending records and shared sources. New draft proposals suggest a project name on acceptance. Optional AI policy review is explicit, budgeted, source-reference checked, cached and visibly stale after changes; clarification options are visible radio cards; document preflight and proposal confirmation use readable wording and named architecture changes. Live document/prompt proposals normalize an unambiguous value such as `TCP 443` to numeric port `443`; ambiguous values remain unknown with a review note, and manual validation errors are human-readable. Live edit context includes bounded deployment IDs/counts/hosts and retrieves only selected project policies.

Live: Terra A/B flows passed in four requests including one repair, estimated $0.0743975. Qwen3:14b small extraction passed (one request); richer flow failed safely (two attempts). Including the new advisory review, eight agent-run validation requests (five cloud, three local) cost an estimated $0.0897775, within the eight-request/$0.50 ceiling. Subsequent user-initiated imports are separate from that total. No escalation, cloud fallback or downloads. Report: `reports/mac-resume-20260921-providers.md`. Unsupported scope found in live-output review was corrected via offline replay and versioned commands in current saved demos; original evidence preserved.

User-import follow-up: `enforcement: "unknown"` now becomes an unspecified list with a review note; known component references are kept and invalid references reject. Nullable unknown enums use the canonical unknown value. Other validation failures show readable field messages. Ollama's user request was still generating at about 12 tokens/second when the 90-second window expired; local default windows now scale to output size, while explicit deadlines stay authoritative and timeouts are not replayed. Tested with mocked transports, without additional live requests. Fresh complete live imports after the fix remain unverified. Evidence: `reports/provider-input-recovery-20260921.md`.

Latest editor/background checkpoint: ten-color palettes for boxes, text, lines and asset icons; movable labels; bounded route clearance; persistent resizing and layer order; grouped policies with optional rule-derived suggestions; prompt-pack import; background jobs and project notifications; cancel/Stopping lifecycle; accepted/rejected proposal history and conditional prompt clearing. Ollama’s fixed 1,536-token output ceiling is replaced with request-specific context allocation (up to 3,720 output tokens in the 8K profile) and a bounded 480-second window; shorter wire instructions, terminal length errors and cancellation preserve usage accounting. No new live calls were made, and a fresh full local generation remains unverified. Evidence: `reports/editor-background-followup-20260921.md`.

Latest follow-up: unified asset/component/connector layer order (including labels), zone-with-contents ordering, independent endpoint controls and SVG parity; remembered Light/Dark mode with readable default colors and unchanged explicit palettes. Browser evidence covers actual overlapping-object hit tests, selection, undo/reopen, both reconnection endpoints, theme persistence and key-surface contrast. `reports/layers-theme-qa/` and `reports/archie-dark-mode.png`. No model calls.

Mini-map follow-up: fixed mismatched internal SVG/panel dimensions that cropped the preview; enabled click-to-centre, drag pan and wheel zoom with a visible viewport outline in both themes. Navigation does not persist architecture edits or invoke a provider.

Browser branding follow-up: the existing robot mascot now serves as the favicon. README and branding provenance are refreshed for this checkpoint. TypeScript, production build and focused browser favicon checks pass in development and the production build (`reports/favicon-qa-20260922.json`). This metadata/documentation change uses no model calls; the application gate above remains the latest full-suite evidence.

Dark-logo follow-up: header and welcome wordmarks now use light lettering and a mint accent on transparent backgrounds, replacing the pale backing cards. Light mode retains the original artwork. Focused browser checks cover both logos, theme reload, switching back to light and unchanged dimensions with no API mutations; screenshots are in `reports/dark-logo-qa/`.

Presentation materials: the main README now embeds the user-supplied overview slide, includes an expandable poster and links to the 24-second demo video. The three original root assets are committed unchanged. Local README links resolve; PNGs were visually inspected and the MP4 metadata confirms 1080p H.264/AAC. This documentation/media update does not change application behavior; the offline gate above remains the latest application verification.

Document retry follow-up: live duplicate detection now requires accepted facts in the current project, rather than merely saved source metadata. A document retained after an unsupported or unaccepted Mock attempt can be explicitly retried with a live provider, keeping its source ID, original evidence and accepted state until review. Accepted duplicates explain project scope and open Sources, where remaining sections can be processed. Cache reuse, preflight and importing the same bytes after Trash/purge are covered with mocked providers; no live model requests were made. Twelve backend regressions and one new browser journey pass; evidence: `reports/document-reimport-20260922.json`. The running Mac API was restarted with no active jobs, and saved project snapshots were unchanged.

PDF evidence follow-up: reproduced the generic excerpt/locator error by flattening a line-wrapped quote from the inventory PDF. Provider citations now restore only whitespace differences within the stated passage and retain its exact original text; changed words, source IDs and locators still reject, with an actionable citation error. Raw responses from the reported failed imports were not retained, so their exact citation mismatch cannot be reconstructed. Eighteen new regressions and 56 focused cases pass; the latest offline run passes 163 backend tests, 33 frontend unit tests and TypeScript. Browser results are 32/33: the existing pointer resize assertion at `apps/web/tests/pointer.spec.ts:15` also fails on an isolated rerun (width remains 185). The production build passes separately. No existing tests were changed. The API was restarted while idle and saved project snapshots and usage records were unchanged. No live provider calls; fresh OpenAI/Ollama imports remain unverified. Evidence: `reports/evidence-citations-20260922.json`, `reports/m7-tests.xml`, `reports/offline-gate.json`. Next task: investigate the pointer resize failure and verify a user-initiated document retry.

SSMS reference recovery: the supplied PDF defines `SYS-VAS` and uses systems as connection endpoints, while canonical connections require components. Batches now carry typed record context; unique ID/name aliases resolve locally and wrong types remain rejected. Document imports reserve one bounded repair inside the existing limits, replace only the offending section response, and retain valid sections. Source-backed system boundary components must be explicitly proposed when internal endpoints are unspecified. Per-response citation checks, identical-add deduplication, clear field errors, cancellation/budget handling and partial coverage are covered by 29 new cases. Latest backend: **192 passed**; TypeScript, 33 frontend unit tests and production build pass. Browser: **32/33**, with the same existing pointer resize failure. An isolated replay parsed all 13 SSMS pages with scripted responses, repaired one bad section and accepted/reopened 3 systems, 18 components (including 3 logical boundaries), 4 zones and 7 connected interfaces; this is not live-model accuracy evidence. No live calls. The idle API was restarted with five saved project snapshots and usage records unchanged. Evidence: `reports/reference-recovery-20260922.json`. Next: user-initiated live retry; separately investigate the pointer resize failure.

Diagram and review follow-up: new document zones now fit their children, and Arrange view uses deterministic compact packing with pinned families/manual-route endpoints preserved, a versioned command and one-step undo. The shared provider prompt asks for a modest source-supported functional breakdown; inferred splits stay proposed, external systems remain opaque, and unknown hardware/transport details stay unknown. A scripted replay of all three pages of the supplied simplified SSMS v1.4 PDF accepts/reopens 8 components, 4 zones and all 11 declared flows; this verifies the contract, not live-model accuracy. AI component symbols now recover from generic server defaults using role/name, while specific catalogue choices survive. The current Sentinel project received 18 icon-only updates in one undoable command; placements, deployments, interfaces and evidence were unchanged. Draft warnings now group scope/port/control issues into named, actionable checklists, with original diagnostics retained. The Inspector adds Icon and Architecture boundary controls. Suggested policies add Select all/Clear all and still require draft acceptance. Validation: **220 backend tests**, **37 frontend unit tests**, TypeScript, production build and **2 focused browser journeys passed**; screenshots were inspected and the live Sentinel canvas was checked. No provider calls were made by this verification; the existing full-browser pointer-resize failure remains separate and was not rerun. The idle API was restarted without changing project snapshots or usage. Evidence: `reports/compact-layout-tests.xml`, `reports/compact-layout-browser.png`, `reports/review-clarity-browser.png`, `reports/functional-decomposition-20260922.json`, `reports/icon-refresh-20260922.json`, `reports/diagram-review-20260922.json`. Next: user-initiated live draft evaluation; separately investigate pointer resize and dense connector labels.

Environment: macOS 26.6.2, M2 Max, 32 GiB, Python 3.12.0, Node 22.23.1, pnpm 11.19.0, Chromium 153.0.8010.12, Ollama 0.34.2. Qwen14B observed 10 GB loaded / 100% GPU / 8192 context, not peak sampling. Historical Windows RTX evidence remains in `reports/windows-checkpoint` and prior smoke reports.

QA: all source DOCX/PDF pages, three narrative pages and three SVG views inspected (`reports/mac-export-qa/review.json`). Browser capacity records 50 components/100 interfaces with intact references and zero provider requests. Initial application JS is about 471 kB; layout no longer loads the ELK chunk. FastAPI startup hooks and the Starlette test client emit deprecation warnings.

Remaining: P0 none known for verified guided Terra demo; P1 dense overlaps/connector crossings beyond bounded clearance routing, no alignment guides, full Ollama reliability, dense Connection details labels obscuring short arrowheads; P2 licensed Windows native Visio and refreshed Windows execution. No native VSDX claim or measured human time savings.

Launch: `bash scripts/dev.sh`; offline gate: `bash scripts/test.sh`; initial dependencies: `bash scripts/setup.sh`. Handover archive excludes `.env`, local databases and dependencies. Next task: native-host Visio verification or scoped routing/local-model work. Do not rerun paid calls for wording/layout.
